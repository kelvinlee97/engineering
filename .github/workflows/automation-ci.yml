name: Automation checks

# Runs on every pull request, with no path filter: it is a required status
# check on main, and a required check that never reports leaves unrelated
# pull requests pending forever. The job takes about 15 seconds.
on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: automation-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint workflows and automation config
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install actionlint
        run: |
          set -euo pipefail
          bash <(curl -fsSL https://raw.githubusercontent.com/rhysd/actionlint/main/scripts/download-actionlint.bash) latest "$RUNNER_TEMP"
          echo "$RUNNER_TEMP" >>"$GITHUB_PATH"

      - name: actionlint
        run: actionlint -color

      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"

      - name: Validate automation config
        run: |
          python -m pip install --quiet pyyaml pytest
          python .github/scripts/validate_automation_config.py

      - name: Test the config validator
        run: python -m pytest .github/scripts/test_validate_automation_config.py -q

      - name: Test the triage rules
        run: node --test .github/scripts/triage.test.js

      - name: Test the changelog generator
        run: .github/scripts/changelog.test.sh
